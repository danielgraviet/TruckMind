[project]
name = "truckmind"
version = "0.1.0"
description = "TruckMind backend"
requires-python = ">=3.13"
dependencies = [
    "anthropic>=0.84.0",
    "fastapi>=0.135.1",
    "httpx>=0.28.1",
    "python-dotenv",
    "requests",
    "uvicorn[standard]",
]

[tool.uv]
package = false
